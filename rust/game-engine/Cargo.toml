[package]
name = "game-engine"
version = "0.1.0"
edition.workspace = true

[dependencies]
shared = { path = "../shared" }
async-trait.workspace = true

[dev-dependencies]
tokio = { workspace = true }
