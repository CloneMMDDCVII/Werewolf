[package]
name = "sim"
version = "0.1.0"
edition.workspace = true

[dependencies]
shared = { path = "../shared" }
game-engine = { path = "../game-engine" }
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
