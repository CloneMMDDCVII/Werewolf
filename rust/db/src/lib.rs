//! Database layer — placeholder. Target: trimmed schema (drop
//! stats/achievement/website tables), `sqlx` against PostgreSQL, migrated
//! from the current SQL Server schema (`werewolf.sql`). Not yet
//! implemented; scaffolded so the workspace's crate boundaries are in
//! place ahead of the actual migration work.
